---
sidebar_position: 2
---

# API Authentication

The Shipstatic REST API uses API keys for authentication. You must include your API key in the `Authorization` header of each request as a Bearer token.

## API Keys

API keys are generated by the platform and always begin with the prefix `ship-`.

To authenticate your API requests, include an `Authorization` header with your API key as a Bearer token:

```
Authorization: Bearer ship-your-api-key
```

## Example

Here's an example of an authenticated request to the `/account` endpoint using `curl`:

```bash
curl -X GET https://api.shipstatic.xyz/account \
  -H "Authorization: Bearer ship-your-api-key"
```
